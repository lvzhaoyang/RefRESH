# This config partly refers to SURREAL datasets
# Zhaoyang Lv

[SHAPENET_MODELS]

tmp_path      = '~/develop/REFRESH/data/ShapeNet/tmp'
output_path   = '~/develop/REFRESH/data/ShapeNet/final'

output_types  = {'depth':True, 'fg':True, 'gtflow':False, 'normal':False, 'vblur':False}

shape_net_ids = ['03790512']

# Set the number of objects in the visible scene
num_obj   = 1

# set the camera parameter
width     = 640
height    = 480

pinhole   = {'fx': 1000, 'fy': 1000, 'cx':320, 'cy':240}

# options for the trajectory type
trajectory  =  {'type': 'ORBIT', 'radius': 3, 'views': 50}

# set the environment lighting distribution (uniform)
env_light_low = 0.8
env_light_high= 1
